language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t raulvidal/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true raulvidal/docker-react npm run test -- --coverage
